# Observation-side resource bound (private /api/observation route owner).
# Installed live with:
#   systemctl --user set-property minecraft-ai-dashboard-live.service \
#     CPUQuota=100% MemoryMax=2G MemorySwapMax=0
#   renice -n 19 -p <dashboard-pid>
[Service]
Nice=19
CPUQuota=100%
MemoryMax=2G
MemorySwapMax=0
